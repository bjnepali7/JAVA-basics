//math class hass many method rhat  allows you to perform the  mathmatical class on numbers


public class Mathclass {
    public static void main(String[] args) {
        System.err.println(Math.max(5, 6));
        System.err.println(Math.min(6, 9));
        System.err.println(Math.sqrt(125));
        System.err.println(Math.random());
        int random=(int)(Math.random()*101);
        System.err.println(random);


    }
    
}
